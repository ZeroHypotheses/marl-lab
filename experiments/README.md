# Experiments

Your own work. One directory per question: `NN-slug/`.

```bash
cp -r _template 01-my-question
```

Then write `HYPOTHESIS.md` **before** writing any code, and follow
[`workflows/experiment.md`](../workflows/experiment.md).

Outputs (`runs/`, `outputs/`, checkpoints) are gitignored. The write-up belongs
in `wiki/notes/` so it's linked into the knowledge base, not stranded here.

| # | Question | Status |
|---|---|---|
| 01 | Can we implement and test tabular IQL in one-step Prisoner's Dilemma? | complete |
| 02 | Does IQL select Stag Hunt's risk-dominant equilibrium across seeds? | complete; hare/hare in 20/20 seeds |
